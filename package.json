{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna clean && rm -rf ./node_modules",
    "test": "echo \"Error: run tests from root\" && exit 1"
  }
}
